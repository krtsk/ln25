// forEach метод массивов, который проходит по всем его элементам и выполняет для каждого 
// из них функцию, переданную в качестве аргумента
const number = [1, 2, 3, 4, 5];
number.forEach (console.log);
number.forEach (alert);

//const animals = ['cat', 'dog', 'elephant', 'tiger', 'lion'];
// const firstWord = animals.find (str.substr(start[7, length]));
// console.log (animals);

const numbers = [-1, -11, -2, -3, -10, -4];

numbers.forEach (alert);